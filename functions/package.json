{
  "name": "functions",
  "description": "Cloud Functions for Firebase",
  "scripts": {
    "lint": "eslint .",
    "serve": "firebase emulators:start --only functions",
    "shell": "firebase functions:shell",
    "start": "npm run shell",
    "deploy": "firebase deploy --only functions",
    "logs": "firebase functions:log",
    "test": "jest -c integration-tests/jest.json --watch --runInBand",
    "test:ci": "jest -c integration-tests/jest.json --runInBand"
  },
  "engines": {
    "node": "10"
  },
  "dependencies": {
    "body-parser": "^1.19.0",
    "cookie-parser": "^1.4.5",
    "cors": "^2.8.5",
    "express": "^4.17.1",
    "firebase-admin": "^8.12.0",
    "firebase-functions": "^3.3.0",
    "models": "file:models",
    "nanoid": "^3.1.6"
  },
  "devDependencies": {
    "eslint": "^5.12.0",
    "jest": "^25.3.0",
    "eslint-plugin-promise": "^4.0.1",
    "firebase-functions-test": "^0.1.7",
    "@firebase/testing": "^0.19.1",
    "lodash.omit": "^4.5.0"
  },
  "private": true
}
